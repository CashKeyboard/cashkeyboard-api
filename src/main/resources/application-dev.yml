spring:
  datasource:
    url: jdbc:h2:mem:cashkeyboard
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop

# 개발 환경에서 사용할 Firebase Admin SDK 설정
firebase:
  credentials:
    path: firebase-service-account-dev.json